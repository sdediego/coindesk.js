/*
 * Coindesk API client.
 */

const { CoindeskAPIClient, CoindeskAPIHttpRequest, CoindeskAPIHttpResponse } = require('./src/coindesk');

module.exports = {
    CoindeskAPIClient,
    CoindeskAPIHttpRequest,
    CoindeskAPIHttpResponse
};